mod accumulator;
mod collection;
mod pipeline;
mod projection;
pub mod sanitize;
mod selection;
mod stage;

#[cfg(test)]
pub mod test_helpers;

pub use self::{
    accumulator::Accumulator,
    collection::CollectionTrait,
    pipeline::Pipeline,
    projection::{ProjectAs, Projection},
    selection::Selection,
    stage::Stage,
};

// MockQueryableCollection is generated by automock when the test flag is active.
#[cfg(test)]
pub use self::collection::MockCollectionTrait;
